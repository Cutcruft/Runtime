type: yamldemo.project
titleField: name
fields:
  - name: name
    type: string
    required: true
    min: 2
    description: Project name
  - name: description
    type: text
  - name: status
    type: enum
    enum: [planning, active, archived]
    default: planning
