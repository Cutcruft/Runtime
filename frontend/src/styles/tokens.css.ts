// Theme tokens — runtime CSS custom properties set by themeStore.applyTheme().
// This file exists as a placeholder for future static token definitions.
// Current tokens are defined via --rt-* CSS custom properties in the browser DOM.
export {}
